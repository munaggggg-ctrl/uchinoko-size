# PROJECT_CONTEXT.md — 会社の記憶

滅多に変わらないことだけを書く。日々の計画は `TODO.md`。

## 何を作っているか

小型犬（10kg未満）の飼い主が、**うちの子の実測値を各ブランドのサイズに翻訳できる**サイト。
`uchinoko-size.com`。中核は記事ではなく道具（`/size-checker/`）。

## なぜ成立するか

ブランドごとにサイズ表の**数値の意味が違う**。「犬の体の実測値」を載せる会社と
「服の仕上がり寸法」を載せる会社があり、同じ35cmが正反対を指す。
公式表を横断して正規化し、**どちらの基準かを原文引用つきで示す**サイトは他にない。

これは公開情報だけで作れる。犬を飼っていなくても、ユーザーがいなくても作れる。
ここが「実際のフィットデータ」を売りにする案との決定的な違い。

## 誰向けか

サイズ選びで一度失敗した飼い主。とくに**胴長犬（ダックス・コーギー）の飼い主**。
胴長用サイズは `D-M` `D3号` `DM` `MD` と表記がバラバラで、横断できる場所がない。

## どう稼ぐか

楽天アフィリエイト（承認済み）。Amazon PA-API は売上実績が要るので後回し。
現時点の売上は0円であり、**事業価値も0円**。ここは正直に置いておく。

## CEOの関与

本業あり。PCに向かえるのは週数回、1回10〜20分。日曜に報告を見る。
**CEOがやること:** WordPress管理画面の操作、`sync.command` の実行、支払い判断、方針判断。
**CEOがやらないこと:** 記事執筆、SNS運用、日次の分析。

## 予算

**月1万円以下。** サーバーは GitHub Actions（無料枠）で、VPSは持たない。
ChatGPT Plus は CEO の普段使いなので、この事業の予算には含めない。

## 絶対にやらないこと

- 出所のない数値の掲載
- モールのスクレイピング
- 健康・医療アドバイス
- AI大量記事の量産
- 規約違反のブラウザBOT

## 使える資産

WordPress（ConoHa WING）／GitHub Actions／楽天API／NotebookLM／
Claude（COO兼CTO）とサブエージェント。既存のドメイン資産・SNSフォロワーは**ゼロ**。

## KPI

いまの唯一の関門は「**検索またはPinterest経由で、他人が到達したセッション10件**」。
1PVは目標として弱すぎる（自分でアクセスすれば達成できてしまう）。
これを30日で超えられないなら、記事量ではなく需要側を疑う。

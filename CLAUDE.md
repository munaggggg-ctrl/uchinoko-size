# CLAUDE.md — この事業で必ず守ること

Claude Code が毎回読み込む。**絶対に守るルールだけを置く。**
背景は `PROJECT_CONTEXT.md`、事故の記録は `KNOWLEDGE.md`、
仕様は散文ではなく `tests/` にある（`python3 tests/run.py`）。

## 数値の扱い（これを破ると事業の存在意義が消える）

1. **出所のない数値を保存しない。** すべての `size_variant` に
   `provenance`（official / estimated / user）と `source_id` が要る。DBのCHECK制約で強制済み。
2. **サイズ表が「犬の実測」か「服の実寸」かを推測しない。**
   公式ページの一文を原文のまま引用できないブランドは、取り込まない。
   ここを間違えると、犬に入らない服を推薦する。実際に IDOG&ICAT と CALULU で
   同じ「35cm」が正反対の意味だった。
3. **AIの推定値を公式値として出さない。** 換算した値は必ず `estimated` + `confidence`。

## 情報源

4. **モール内ページを根拠にしない。** 楽天市場・Amazon・Yahoo!ショッピング、
   および `rakuten.ne.jp`（楽天GOLD）・`shopping.geocities.jp` は不可。
   ブランドが自社運営するECドメインは可。
5. **レビュー本文をスクレイピングしない。** 規約違反。口コミは自前で集める。
6. **API代の節約を理由に、規約違反のブラウザBOTを作らない。**

## 認証情報

7. **APIキー・トークンをコードやファイルに直書きしない。** GitHub Secrets のみ。
8. **DB接続情報は使わない。** WordPress の自動化は REST API ＋
   アプリケーションパスワードだけで行う。
9. **Claude はパスワードを入力しない。** ログインが要る操作は CEO に依頼する。

## 公開

10. **`pipeline/lint.py` を通らないものを公開しない。** ERROR があれば止める。
11. **既存ページの公開状態を下書きへ戻さない。** 昇格（draft→publish）は
    `PUBLISH_STATUS=publish` を明示したときだけ。一度これで公開中のページを404にした。
12. **AI大量記事サイトにしない。** 記事の中心は文章ではなく、
    他所にない表。表を作れない記事は書かない。
13. **健康・医療は扱わない。** 効能を述べない。診断・投薬・受診要否に踏み込まない。

## 作業の進め方

14. **コードを変えたら `python3 tests/run.py` を必ず通す。** 落ちたまま同期しない。
15. **GitHubへの送信は CEO が `sync.command` を押したときだけ。** まとめて渡す。
16. **事故を起こしたら `KNOWLEDGE.md` に追記してから次へ進む。**
    同じ壊れ方を二度しないための回帰テストも同時に足す。
17. **CEO に確認を求めるのは、CEO にしかできない操作のときだけ。**
    技術的な判断は自分で下して、理由とともに報告する。

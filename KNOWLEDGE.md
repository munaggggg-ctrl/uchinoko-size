# KNOWLEDGE.md — 事故と、その対処

**追記のみ。消さない。** 一度払った授業料を二度払わないための記録。
新しい事故を起こしたら、ここに書いてから次へ進む。回帰テストも同時に足す。

---

## サイズ表の基準が会社ごとに違う（事業の中核）

CALULU S の胴回り「31〜34cm」は**犬の適合レンジ**。
IDOG&ICAT S の胴周り「35cm」は**服そのものの寸法**。
同じ列に並べると判定が丸ごと壊れる。IDOGのSを適合レンジと誤読すると、
胴回り35cmの犬にSを勧めるが、実際には入らない。

→ `size_chart.measure_basis` で持ち、`pipeline/normalize.py` が実寸表記を犬基準へ変換する。
   変換結果は必ず `provenance='estimated'` + `confidence=0.6`。
   ゆとり値は milla milla の公開値を使う（自分で発明しない）。

**列ごとに基準が違うブランドもある。** PET PARADISE は首囲・胴囲が犬基準、
着丈だけ商品ごとの実寸だった（7商品を突き合わせて判明）。
DBは表ごとに1基準しか持てないので、犬基準の列だけ取り込み、着丈は NULL にした。

---

## 楽天API が 403 を返す（Referer だけでは足りない）

`REQUEST_CONTEXT_BODY_HTTP_REFERRER_MISSING`。4回連続で失敗した。
GitHub Actions 上で6通り試して切り分けた結果:

| 送ったヘッダ | 結果 |
|---|---|
| なし | 403 |
| Referer のみ | 403 |
| Referer + ブラウザUA | 403 |
| **Referer + Origin** | **200** |

→ **両方必須。** 公式ドキュメントには書かれていない。`pipeline/rakuten.py` で両方送る。

---

## ConoHa の「海外アクセス制限 → REST-API」が自動投稿を止める

GitHub Actions はアメリカのサーバーから動くので、日本国外からのアクセスとして
Apache が 403 のHTMLを返す。WordPressのJSONエラーではないので、認証の問題と誤診しやすい。

→ **サイトセキュリティ → WordPressセキュリティ → 海外アクセス制限 → REST-API を OFF。**
   ダッシュボード・XML-RPC・ログイン試行回数制限は **ON のまま**にしておけば、
   管理画面は海外から閉じたまま自動投稿だけが通る。

**見分け方:** 認証エラーならWordPressがJSONを返す。ApacheのHTMLが返ってきたら、
PHPに届く前にサーバーで弾かれている＝認証は無関係。

---

## `wpautop` が本文中の JavaScript を壊す

WordPress は本文の空行を `</p><p>` に置き換える。それが `<script>` の中にも入り込み、
JavaScript が構文エラーで丸ごと止まった。入力欄は出るのに結果が出ない状態になる。

→ 配信するCSSとJSから**改行を全部なくす**（`toolpage.py` の `_oneline`）。
   さらに `<!-- wp:html -->` で包む（ブロックがあると wpautop が外れる）。二重の保険。
   行コメント（`//`）が混入すると1行化で全部コメントになるため、混入したらビルドを止める。

---

## 更新のたびに公開ページを下書きへ戻して 404 にした

`publish()` が毎回 `status: draft` を送っていたため、CEOが「公開」にしたページを
自動更新が引き戻していた。

→ **公開状態は一方向にしか動かさない。** 下書き→公開は `PUBLISH_STATUS=publish` を
   明示したときだけ。公開→下書きは決してしない。

---

## `DAILY_TOKEN_CAP` が効いていなかった

ワークフローは `DAILY_TOKEN_CAP` を渡していたのに、`collect.py` が読んでいたのは
`COLLECT_DAILY_CAP` という別名だった。しかも数えていたのは**トークンではなくリクエスト件数**。
画像1枚が数千トークンなので、件数上限では費用の上限にならない。

→ 応答の `usageMetadata.totalTokenCount` を積算して止める。`maxOutputTokens` も設定。
   **「上限を設けた」と書いてあることと、上限が効いていることは別。**

---

## 判定エンジンは必ず「最も惜しいサイズ」を返す

まったく合わない体重でも表の行は埋まる。放っておくと
「体重20kgの小型犬に合う犬服｜7ブランド横断比較」という全行×の記事が出てしまう。

→ 適合（×以外）が3ブランド未満なら記事を作らない。
   ×の行自体は「このブランドには無い」という情報なので、表からは消さない。

---

## 認証情報をリポジトリに入れかけた

`.token` と `.setup_secrets.py`（楽天アクセスキー入り）が `git ls-files` に出ていた。
別のエラーでスクリプトが止まっていたおかげで、実際には漏れなかった。

→ `.gitignore` に追加し、`git grep` で履歴を確認。
   **平文のトークンをディスクに置かない。** 同期の認証は macOS キーチェーンに預ける。

---

## 収集で使えなかったブランドと理由

| ブランド | 理由 |
|---|---|
| RADICA | 自社ドメインにサイズ表が無く、モール内ページにしか存在しない |
| Mandarine Brothers | サイズ表が全て画像。数値がテキスト化されていない |
| DOG PEACE | 犬の実測か服の実寸かを明記した一文が公式に無い（状況証拠は強いが不採用） |
| as know as de wan | 画像のみ。かつ犬基準と服基準の2系統の定義が同一ページに併記 |

**サイズ表が画像で提供されているブランドが多い。** これが収集の最大の壁。
OCRを使う場合も、原文引用が取れなければ取り込まない方針は変えない。
